# ZenGen · a digital garden

基于 [Quartz v5](https://quartz.jzhao.xyz) 构建的数字花园，由 Obsidian 笔记驱动。

📎 **地址**: https://limn-d9y.pages.dev

## 本地开发

```bash
npm install
npx quartz build --serve
```

## 部署

```bash
npx quartz build
```

输出在 `public/` 目录，部署到 Cloudflare Pages。
